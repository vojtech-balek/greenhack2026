# Frontend

Static browser UI for `renovuj.me`.

- `index.html` contains the page structure.
- `styles.css` contains the visual system and responsive layout.
- `app.js` renders navigation data and reads placeholder backend status.
